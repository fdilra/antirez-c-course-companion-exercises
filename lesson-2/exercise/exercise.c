#include <stdio.h>

/* 
 * Exercise: The following program has several problems.
 * It should:
 *   1. Print the sum of 10 and 20
 *   2. Print the difference of 20 and 10
 * 
 * Read the error messages when compiling and fix the issues.
 */

int sum(int a, int b) {
    // TODO: return the sum of a and b
}

// TODO: fix this function.
int diff() {
    return b - a;
}

int main() {
    // TODO: declare and initialize the variables a and b correctly
    a = 10;
    b = 20;

    // TODO: fix the format specifier in this printf() call
    printf("The sum of 10 and 20 is: %s\n", sum(a, b));

    // TODO: fix the arguments in this diff() call
    printf("The difference of 20 and 10 is: %d\n", diff());
    
    return 0;
}
